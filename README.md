# Simple Weather App

A small Next.js weather app for a React workshop. It uses Open-Meteo, which does not require an API key.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.

The app lives in `app/page.jsx`, and the display component is in `app/component.jsx`.
